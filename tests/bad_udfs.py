def not_a_plotting_func(x: float) -> float:
    """This is not a plotting function.

    It actual squares the parameter ``x``. Oh dear, if I wanted to do
    this, I should have made the function private. Oh well.
    """
    return x * x
